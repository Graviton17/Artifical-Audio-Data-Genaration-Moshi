# Core
numpy>=1.24
pyyaml>=6.0
soundfile>=0.12
sphn==0.1.12          # duration reader used by the manifest (matches Data-Processing-Moshi)

# ML / TTS / alignment
torch>=2.1
transformers>=4.40
ctc-forced-aligner    # multilingual (MMS-based) forced alignment of KNOWN text -> word timings
                      # if unavailable for your platform, the HeuristicAligner fallback is used.

# The concrete TTS model (IndicMioTTS) pulls its own weights at runtime via transformers.
# Swap the model id in config.yaml if `SPRINGLab/Indic-Mio` does not resolve.

# LLM providers (conversation-generation agents)
google-genai>=1.0     # GeminiLLM
groq>=0.11            # GroqLLM
openai>=1.0           # OpenAILLM
requests>=2.31        # KrutrimLLM, SarvamLLM, InceptionLLM (plain REST, no vendor SDK)

# LANGFUSE
langfuse>=2.0

# Storage (production conversation bucket + checkpoint on HuggingFace Hub)
huggingface_hub>=1.5.0   # bucket API: create_bucket / batch_bucket_files / download_bucket_files

# Metrics (token usage + conversation/instance progress dashboards)
wandb>=0.17
